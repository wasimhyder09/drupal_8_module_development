<?php
/**
 * Implements hook_form_alter().
 */

function wasim_form_alter(&$form, Drupal\Core\Form\FormStateInterface $form_state, $form_id) {
  if($form_id == 'comment_comment_form') {
    $form['actions']['preview']['#value'] = t('Show me first');
  }
}